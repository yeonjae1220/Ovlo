apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ovlo-postgres-pvc
  namespace: ovlo
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: ovlo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      nodeSelector:
        project2: ovlo
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "ovlo"
            - name: POSTGRES_USER
              value: "ovlo"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ovlo-secret
                  key: POSTGRES_PASSWORD
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "ovlo"]
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: ovlo-postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: ovlo
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
