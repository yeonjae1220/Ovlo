apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ovlo-media-pvc
  namespace: ovlo
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
  namespace: ovlo
spec:
  replicas: 1
  # strategy 를 일부러 지정하지 않는다(= RollingUpdate). RWO PVC(업로드 미디어)를 쓰지만
  # GLOBAL-PIT-095 에 해당하지 않는다 — 2026-09-07 실측 근거 셋:
  #   ① 이 컨테이너는 데이터 디렉터리를 배타 락하지 않는다(postgres/minio 와 다름).
  #      구·신 파드가 잠시 함께 떠도 서로를 막지 않는다.
  #   ② local-path PV 에 spec.nodeAffinity.required 가 이미 박혀 있어, 노드가 늘어도
  #      스케줄러가 이 PVC 를 쓰는 파드를 그 노드 밖에 배치하지 못한다.
  #      따라서 파드 레벨 nodeSelector/affinity 는 같은 제약을 두 벌 적는 것이라 불필요하다.
  #   ③ 실제로 130세대까지 정상 롤아웃됐다.
  # 단일 레플리카 DB 와 달리 여기서는 RollingUpdate 가 진짜 무중단을 준다.
  # Recreate 로 바꾸면 배포마다 앱 기동 시간만큼 다운타임이 생긴다.
  selector:
    matchLabels:
      app: app
  template:
    metadata:
      labels:
        app: app
    spec:
      nodeSelector:
        project2: ovlo
      containers:
        - name: app
          image: ghcr.io/yeonjae1220/ovlo:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres:5432/ovlo"
            - name: SPRING_DATASOURCE_USERNAME
              value: "ovlo"
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ovlo-secret
                  key: POSTGRES_PASSWORD
            - name: SPRING_DATASOURCE_DRIVER
              value: "org.postgresql.Driver"
            - name: JPA_DIALECT
              value: "org.hibernate.dialect.PostgreSQLDialect"
            - name: SPRING_DATA_REDIS_HOST
              value: "redis"
            - name: SPRING_DATA_REDIS_PORT
              value: "6379"
            - name: SPRING_DATA_REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ovlo-secret
                  key: REDIS_PASSWORD
            # redis 인증을 켜려면 sts args 의 --requirepass 와 이 env 를 **함께** 바꿔라.
            - name: FLYWAY_ENABLED
              value: "true"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: ovlo-secret
                  key: JWT_SECRET
            - name: CORS_ALLOWED_ORIGINS
              valueFrom:
                secretKeyRef:
                  name: ovlo-secret
                  key: CORS_ALLOWED_ORIGINS
            - name: GOOGLE_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: ovlo-secret
                  key: GOOGLE_CLIENT_ID
            - name: GOOGLE_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: ovlo-secret
                  key: GOOGLE_CLIENT_SECRET
            - name: ADMIN_EMAIL
              valueFrom:
                secretKeyRef:
                  name: ovlo-secret
                  key: ADMIN_EMAIL
            - name: ADMIN_PASSWORD_BCRYPT
              valueFrom:
                secretKeyRef:
                  name: ovlo-secret
                  key: ADMIN_PASSWORD_BCRYPT
            # lab.mungji 콘솔 집계용 서비스 토큰 — 미설정 시 /api/internal/** 차단
            - name: CONSOLE_INTERNAL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: ovlo-secret
                  key: CONSOLE_INTERNAL_TOKEN
            - name: LAB_FEEDBACK_TOKEN
              valueFrom:
                secretKeyRef:
                  name: ovlo-secret
                  key: LAB_FEEDBACK_TOKEN
            # 이메일(SMTP) — optional:true. 시크릿에 키가 없으면 env 미설정 → spring.mail.host 부재 →
            # LogEmailSenderAdapter 폴백. 4개 키가 모두 채워지면 SmtpEmailSenderAdapter(Gmail) 활성화.
            - name: SPRING_MAIL_HOST
              valueFrom:
                secretKeyRef:
                  name: ovlo-secret
                  key: SPRING_MAIL_HOST
                  optional: true
            - name: MAIL_USERNAME
              valueFrom:
                secretKeyRef:
                  name: ovlo-secret
                  key: MAIL_USERNAME
                  optional: true
            - name: MAIL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ovlo-secret
                  key: MAIL_PASSWORD
                  optional: true
            - name: MAIL_FROM
              valueFrom:
                secretKeyRef:
                  name: ovlo-secret
                  key: MAIL_FROM
                  optional: true
            # nginx-ingress 파드의 IP에서 온 요청만 X-Real-IP를 신뢰 (스푸핑 방지).
            # k3s/flannel 기본 파드 CIDR — 운영 환경에서 `kubectl get pods -n kube-system -o wide`로
            # 실제 ingress-nginx 파드 대역을 확인해 필요 시 갱신할 것 (CIDR 표기 지원).
            - name: TRUSTED_PROXY_IPS
              value: "127.0.0.1,::1,10.42.0.0/16"
            # ── 아래는 라이브에서 회수한 값이다 (GLOBAL-PIT-083) ──────────────
            # 이 앱의 CI 는 `kubectl set image` 형이라 이 매니페스트를 apply 하지 않는다
            # (GLOBAL-PIT-036). 그래서 여기 적힌 값은 저절로 라이브에 닿지 않고,
            # 반대로 라이브에만 있던 값은 누군가 수동 apply 하는 순간 조용히 사라진다.
            # 2026-09-08 실측(`kubectl get deploy -o json`)으로 회수했다.
            - name: OTEL_SDK_DISABLED
              value: "false"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector.monitoring.svc.cluster.local:4318"
            - name: OTEL_SERVICE_NAME
              value: "ovlo-backend"
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "service.name=ovlo-backend,service.namespace=ovlo,deployment.environment=production"
            - name: OTEL_LOGS_EXPORTER
              value: "otlp"
            - name: OTEL_METRICS_EXPORTER
              value: "otlp"
            - name: OTEL_TRACES_EXPORTER
              value: "otlp"
            - name: JAVA_TOOL_OPTIONS
              value: "-XX:MaxRAMPercentage=75.0 -XX:InitialRAMPercentage=50.0 -XX:+UseG1GC"
          volumeMounts:
            - name: media-data
              mountPath: /tmp/ovlo-media
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 90
            periodSeconds: 15
            failureThreshold: 3
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
      volumes:
        - name: media-data
          persistentVolumeClaim:
            claimName: ovlo-media-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: ovlo
spec:
  selector:
    app: app
  ports:
    - port: 8080
      targetPort: 8080
