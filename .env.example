# Docker 실행 전 아래 파일을 .env로 복사하고 JWT_SECRET을 설정하세요.
# cp .env.example .env

# JWT 서명 키 (최소 32바이트, Base64 인코딩 권장)
# 생성 예시: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-this-in-production-minimum-32-bytes
